#!/usr/bin/env python# -*- coding: utf-8 -*-#战斗模拟器from ensurepip import __main__flog_move = 1flog_attack = 2flog_stop = 3class Person():    def __init__(self,warmap,testID,state = flog_stop,blood = 1000,armor = 10,attack = 110,aspeed = 110,mspeed= 100,arange = 100):        self.warmap = warmap        self.testID = testID        self.state = state        self.blood = blood        self.armor = armor        self.attack = attack        self.aspeed = aspeed        self.mspeed = mspeed        self.arange = arange        self.px = 0        self.py = 0                self.attackTimeOn = 0        self.attackTimeOff = 0                        class Warmap():    def __init__(self,width,height):        self.width = width        self.height = height        self.person1s = []        self.person2s = []        self.warNotEnd = True    def setAttackPerson(self,person1s,person2s):        self.person1s = person1s        self.person2s = person2s    def start(self):        self.warloop()    def warloop(self):        while self.warNotEnd:            passif  __name__ == '__main__':    print 'test'    timex = [1,4,6,2,9,5,3]    persons = []    for x in timex:        p = Person(None,x)        persons.append(p)    for p in persons:        print p.testID,';'    persons.sort(key = lambda x:x.testID)    for p in persons:        print p.testID,';'    