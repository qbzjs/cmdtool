#!/usr/bin/env python# -*- coding: utf-8 -*-#python 按位异或a = 'abcdefgh'secstr = 'mima'def ecodeAndDeCodeWithCodeStr(tstr,secStr):    seclen = len(secStr)    strout = ''    tnum = 0    for c in tstr:        if tnum >= seclen:            tnum = 0        strout += chr(ord(c) ^ ord(secStr[tnum]))        tnum += 1    return stroutprint a codestr = ecodeAndDeCodeWithCodeStr(a, secstr)print codestrdcodestr = ecodeAndDeCodeWithCodeStr(codestr, secstr)print dcodestr